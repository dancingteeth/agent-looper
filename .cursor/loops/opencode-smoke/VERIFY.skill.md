# Verify skill — opencode-smoke

Run the shell verifier:

```bash
bash .cursor/loops/opencode-smoke/verify.sh
```

If it fails, create or fix `.cursor/loops/opencode-smoke/probe.txt` per `GOAL.md`, then rerun until exit 0.
